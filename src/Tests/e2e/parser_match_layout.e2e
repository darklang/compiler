// Nested multiline matches must retain their dedented outer fallback arm.

let nestedFunctionListMatch<'a> (list: List<a -> a -> Int>) : Int =
    match list with
    | [comparator] ->
        match list with
        | [inner] -> 1
        | _ -> 2
    | _ -> 0

nestedFunctionListMatch<Int> ([]) = 0
