version: 2

project:
  name: dark-compiler

gates:
  - name: build
    run: ./build --ai
  - name: tests
    run: ./run-tests --ai
  - name: benchmarks
    run: ./benchmarks/run_benchmarks.sh --verify full
